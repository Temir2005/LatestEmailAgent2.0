## Git and Version Control Restrictions
- CRITICAL: Never execute any git commands (e.g., git add, git commit, git push, git checkout).
- Do not attempt to manage branches, resolve merge conflicts, or look at git histories.
- All version control tasks must be left exclusively to the user.
- If a task requires saving or committing changes, prompt the user to handle the Git operations manually.
